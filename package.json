{
  "name": "cartridge-forge",
  "version": "0.1.0",
  "description": "AI-driven turn-based game generator using ECS architecture and declarative DSL cartridges",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "validate": "tsx src/cartridge/cli-validate.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "expr-eval": "npm:expr-eval-fork@^3.0.3",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^4.1.4"
  }
}
